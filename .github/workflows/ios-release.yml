name: iOS Release

on:
  push:
    branches:
      - feat/gitHubAction2

jobs:
  build-and-deploy:
    runs-on: macos-latest
    
    steps:
    - uses: actions/checkout@v4
      
    - name: Select Xcode 16
      uses: maxim-lobanov/setup-xcode@v1
      with:
        xcode-version: '16.2.0'

    - name: Install the Apple certificate and provisioning profile
      env:
        BUILD_CERTIFICATE_BASE64: ${{ secrets.BUILD_CERTIFICATE_BASE64 }}
        P12_PASSWORD: ${{ secrets.P12_PASSWORD }}
        BUILD_PROVISION_PROFILE_BASE64: ${{ secrets.BUILD_PROVISION_PROFILE_BASE64 }}
        KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
      run: |
        # create variables
        CERTIFICATE_PATH=$RUNNER_TEMP/build_certificate.p12
        PP_PATH=$RUNNER_TEMP/build_pp.mobileprovision
        KEYCHAIN_PATH=$RUNNER_TEMP/app-signing.keychain-db

        # import certificate and provisioning profile from secrets
        echo -n "$BUILD_CERTIFICATE_BASE64" | base64 --decode -o $CERTIFICATE_PATH
        echo -n "$BUILD_PROVISION_PROFILE_BASE64" | base64 --decode -o $PP_PATH

        # create temporary keychain
        security create-keychain -p "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH
        security set-keychain-settings -lut 21600 $KEYCHAIN_PATH
        security unlock-keychain -p "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH

        # import certificate to keychain
        security import $CERTIFICATE_PATH -P "$P12_PASSWORD" -A -t cert -f pkcs12 -k $KEYCHAIN_PATH
        security set-key-partition-list -S apple-tool:,apple: -k "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH
        security list-keychain -d user -s $KEYCHAIN_PATH

        # apply provisioning profile
        mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
        cp $PP_PATH ~/Library/MobileDevice/Provisioning\ Profiles

    - name: Update App Version
      run: |
        # Get current version and build number from project settings
        CURRENT_VERSION=$(xcodebuild -project FlipCam/FlipCam.xcodeproj -scheme FlipCam -configuration Release -showBuildSettings | grep MARKETING_VERSION | cut -d'=' -f2 | xargs)
        
        # Log if version or build number is not set
        if [ -z "$CURRENT_VERSION" ]; then
          echo "Warning: MARKETING_VERSION is not set in project settings"
        fi
        
        # Append workflow run number to build number
        NEW_VERSION="${CURRENT_VERSION}${GITHUB_RUN_NUMBER}"
        # Update both version and build number to be the same
        xcodebuild -project FlipCam/FlipCam.xcodeproj \
          -scheme FlipCam \
          -configuration Release \
          MARKETING_VERSION=$NEW_VERSION \
          CURRENT_PROJECT_VERSION=$NEW_VERSION
        
        echo "Updated version: ${CURRENT_VERSION} (${NEW_VERSION})"

#    - name: Build and Test
#      run: |
#        xcodebuild clean build test \
#          -project FlipCam/FlipCam.xcodeproj \
#          -scheme FlipCam \
#          -destination 'platform=iOS Simulator,name=iPhone 16 Pro Max,OS=18.2' \
#          CODE_SIGN_IDENTITY="" \
#          CODE_SIGNING_REQUIRED=NO
          
    - name: Build and Archive
      run: |
        xcodebuild clean archive \
          -project FlipCam/FlipCam.xcodeproj \
          -scheme FlipCam \
          -configuration Release \
          -archivePath ./build/FlipCam.xcarchive \
          CODE_SIGN_IDENTITY="$CERT_IDENTITY" \
          CODE_SIGNING_REQUIRED=YES \
          DEVELOPMENT_TEAM="XN354JRCK2"
          
    - name: Export IPA
      run: |
        xcodebuild -exportArchive \
          -archivePath ./build/FlipCam.xcarchive \
          -exportOptionsPlist ./exportOptions.plist \
          -exportPath ./build \
          -allowProvisioningUpdates
          
    - name: Upload to App Store Connect
      uses: apple-actions/upload-testflight-build@v1
      with:
        app-path: "./build/FlipCam.ipa"
        issuer-id: ${{ secrets.APP_STORE_CONNECT_ISSUER_ID }}
        api-key-id: ${{ secrets.APP_STORE_CONNECT_KEY_ID }}
        api-private-key: ${{ secrets.APP_STORE_CONNECT_PRIVATE_KEY }} 
